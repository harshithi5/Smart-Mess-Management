# Smart-Mess-Management

Frontend:
    public folder is scrap.
    all the images + svg are in src\assets

    Rendering order:
    App -> Mainpage
    MainPage -> Navbar01 + Welcome + Features + PopularAuthors
    Need to connect these two, main page and the dashboard
    Dashboard